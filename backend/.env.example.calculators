# Calculators Module Feature Flag
# Controls whether the calculators module is loaded.
# Default: true (module loads at startup, controllers delegate to services)
# Set to "false" to bypass the module and fall back to direct controller logic.
CALCULATORS_MODULE_ENABLED=true
